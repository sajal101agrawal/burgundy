{
  "name": "@concierge/routing-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@concierge/db": "workspace:*",
    "@concierge/logger": "workspace:*",
    "@concierge/types": "workspace:*",
    "@concierge/interrupt-classifier": "workspace:*",
    "@concierge/proactive-messaging": "workspace:*",
    "drizzle-orm": "^0.36.4"
  }
}
