{
  "name": "@concierge/email-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@concierge/queue": "workspace:*",
    "@concierge/logger": "workspace:*",
    "bullmq": "^5.58.5",
    "ioredis": "^5.6.1",
    "fastify": "^4.28.1"
  }
}
