{
  "name": "@concierge/vault-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@concierge/logger": "workspace:*",
    "@concierge/types": "workspace:*",
    "fastify": "^4.28.1"
  }
}
